# create -  docker compose -f docker/docker-compose.postgres.yml up -d 
version: '3.8'

name: invento

services:
  db:
    image: postgres:16
    container_name: pg-invento
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: invento
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata: